export interface Country {
  name: string;
  code: string;
  flag: string;
}

export interface MedicalAuthority {
  id: string;
  name: string;
}

export interface Profession {
  id: string;
  name: string;
}