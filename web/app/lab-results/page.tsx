"use client";

import Link from 'next/link';
import { ArrowLeft, FlaskConical, AlertTriangle, CheckCircle2 } from 'lucide-react';
import { useEffect, useState } from 'react';
import ProtectedRoute from '@/components/auth/protected-route';
import { patientClinicalRecordsService } from '@/services/patient-clinical-records.service';

function date(value: unknown) { if (!value) return 'Not recorded'; const d = new Date(String(value)); return Number.isNaN(d.getTime()) ? String(value) : d.toLocaleDateString('en-ZA', { day: 'numeric', month: 'long', year: 'numeric' }); }

export default function LabResultsPage() {
  const [items, setItems] = useState<any[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
  useEffect(() => { patientClinicalRecordsService.get().then((data) => setItems(data.labResults ?? [])).catch(() => setError("We couldn't load your laboratory results.")).finally(() => setLoading(false)); }, []);
  return <ProtectedRoute><main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8"><Link href="/health-records" className="mb-6 inline-flex items-center gap-2 text-sm font-medium text-[#0b2d54] hover:text-[#24c1c4]"><ArrowLeft className="h-4 w-4" />Health records</Link><div className="mb-8"><div className="mb-3 inline-flex items-center gap-2 rounded-full bg-[#24c1c4]/10 px-3 py-1 text-xs font-semibold text-[#0b2d54]"><FlaskConical className="h-3.5 w-3.5" />Clinical results</div><h1 className="text-3xl font-bold tracking-tight text-[#0b2d54] sm:text-4xl">Laboratory results</h1><p className="mt-2 max-w-2xl text-slate-500">Review released laboratory results and see individual measurements that have been recorded in your health record.</p></div>{loading && <div className="rounded-2xl bg-white p-6 text-sm text-slate-500">Loading your laboratory results…</div>}{error && <div className="rounded-2xl border border-red-200 bg-white p-6 text-sm text-red-700">{error}</div>}{!loading && !error && items.length === 0 && <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center"><FlaskConical className="mx-auto h-9 w-9 text-slate-400" /><h2 className="mt-4 font-semibold text-[#0b2d54]">No laboratory results yet</h2><p className="mt-2 text-sm text-slate-500">Results will appear here after laboratory testing is recorded and released.</p></div>}{!loading && !error && items.length > 0 && <div className="space-y-4">{items.map((result) => { const abnormal = (result.items ?? []).some((item: any) => item.abnormal || item.critical); return <article key={result.id} className={`rounded-2xl border bg-white p-6 shadow-sm ${abnormal ? 'border-amber-200' : 'border-slate-200'}`}><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-semibold text-[#0b2d54]">{result.orderItem?.test?.name || 'Laboratory test'}</h2><span className="rounded-full bg-slate-100 px-2.5 py-1 text-[11px] font-semibold text-slate-600">{String(result.status ?? 'PENDING').replace(/_/g, ' ')}</span>{abnormal && <span className="inline-flex items-center gap-1 rounded-full bg-amber-50 px-2.5 py-1 text-[11px] font-semibold text-amber-700"><AlertTriangle className="h-3 w-3" />Review result</span>}</div><p className="mt-2 text-sm text-slate-500">{result.orderItem?.order?.laboratory?.name || 'Laboratory'} · Reported {date(result.reportedAt)}</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{(result.items ?? []).map((item: any) => <div key={item.id} className={`rounded-xl p-4 ${item.critical ? 'bg-red-50' : item.abnormal ? 'bg-amber-50' : 'bg-slate-50'}`}><div className="flex items-center justify-between gap-2"><p className="text-xs font-medium text-slate-500">{item.test?.name || 'Result'}</p>{item.critical || item.abnormal ? <AlertTriangle className="h-4 w-4 text-amber-600" /> : <CheckCircle2 className="h-4 w-4 text-[#24c1c4]" />}</div><p className="mt-2 text-lg font-bold text-[#0b2d54]">{item.numericValue ?? item.textValue ?? (item.booleanValue != null ? String(item.booleanValue) : item.dateValue ? date(item.dateValue) : '—')}</p>{item.comments && <p className="mt-1 text-xs text-slate-500">{item.comments}</p>}</div>)}</div>{result.specimen?.specimenType?.name && <p className="mt-4 text-xs text-slate-400">Specimen: {result.specimen.specimenType.name}</p>}</article>})}</div>}</div></main></ProtectedRoute>;
}
