"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, CalendarClock, ChevronRight, FileText, Stethoscope } from "lucide-react";
import ProtectedRoute from "@/components/auth/protected-route";
import { api } from "@/lib/api";

function formatDate(value?: string) { if (!value) return "—"; const d = new Date(value); return Number.isNaN(d.getTime()) ? value : new Intl.DateTimeFormat("en-ZA", { dateStyle: "medium", timeStyle: "short" }).format(d); }

export default function EncountersPage() {
  const [encounters, setEncounters] = useState<any[]>([]); const [loading, setLoading] = useState(true);
  useEffect(() => { (async () => { try { const response = await api.get<{ data: any }>("/health-home/timeline"); setEncounters((response.data.data ?? response.data).encounters ?? []); } finally { setLoading(false); } })(); }, []);
  return <ProtectedRoute><main className="min-h-screen bg-[#f5f8fb]"><header className="border-b bg-white"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-4"><div className="flex items-center gap-3"><Link href="/dashboard" className="rounded-xl p-2 hover:bg-slate-100"><ArrowLeft className="h-5 w-5 text-slate-500" /></Link><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#24c1c4]">My Health</p><h1 className="font-bold text-[#0b2d54]">Encounters</h1></div></div><Link href="/health-timeline" className="flex items-center gap-2 rounded-xl bg-[#0b2d54] px-4 py-2 text-xs font-bold text-white">Health Timeline <ChevronRight className="h-3.5 w-3.5" /></Link></div></header><div className="mx-auto max-w-6xl space-y-6 px-4 py-8"><section className="rounded-[28px] border border-[#24c1c4]/20 bg-white p-7 shadow-sm"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-[#24c1c4]/10 text-[#0b2d54]"><Stethoscope className="h-6 w-6" /></div><h2 className="mt-4 text-3xl font-bold text-[#0b2d54]">Care encounters</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Your consultations and clinical interactions, including assessments, notes, vitals, diagnoses and procedures recorded during care.</p></section>{loading ? <div className="space-y-4">{[1,2,3].map((n) => <div key={n} className="h-36 animate-pulse rounded-3xl bg-white" />)}</div> : encounters.length === 0 ? <div className="rounded-3xl border border-dashed bg-white p-10 text-center"><FileText className="mx-auto h-8 w-8 text-[#24c1c4]" /><h3 className="mt-4 font-bold text-[#0b2d54]">No encounters recorded yet</h3><p className="mt-2 text-sm text-slate-500">Your future consultations will appear here automatically.</p></div> : <div className="space-y-4">{encounters.map((encounter) => <article key={encounter.id} className="rounded-3xl border border-slate-200/80 bg-white p-5 shadow-sm"><div className="flex gap-4"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-[#0b2d54]/5 text-[#0b2d54]"><Stethoscope className="h-5 w-5" /></div><div className="flex-1"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wide text-[#24c1c4]">{encounter.encounterType?.name ?? "Clinical encounter"}</p><h3 className="mt-1 font-bold text-[#0b2d54]">{encounter.chiefComplaint ?? "Care encounter"}</h3></div></div><div className="mt-4 flex flex-wrap gap-4 text-xs text-slate-500"><span className="flex items-center gap-2"><CalendarClock className="h-3.5 w-3.5" />{formatDate(encounter.startedAt)}</span><span className="flex items-center gap-2"><Stethoscope className="h-3.5 w-3.5" />{encounter.practitioner?.person ? `${encounter.practitioner.person.firstName} ${encounter.practitioner.person.lastName}` : "Care team not recorded"}</span></div>{encounter.assessment && <div className="mt-4 rounded-2xl bg-[#f7fafc] p-4"><p className="text-[10px] font-bold uppercase tracking-wide text-slate-400">Assessment</p><p className="mt-1 text-sm leading-6 text-slate-600">{encounter.assessment}</p></div>}{encounter.plan && <div className="mt-3 rounded-2xl border border-slate-100 p-4"><p className="text-[10px] font-bold uppercase tracking-wide text-slate-400">Care plan</p><p className="mt-1 text-sm leading-6 text-slate-600">{encounter.plan}</p></div>}</div></div></article>)}</div>}</div></main></ProtectedRoute>;
}
