"use client";

import Link from "next/link";
import { ArrowLeft, Activity, FileText, FlaskConical, Image as ImageIcon, ShieldCheck } from "lucide-react";
import { useDashboard } from "@/hooks/use-dashboard";

function formatDate(value: unknown) { if (!value) return "Date not recorded"; const date = new Date(String(value)); if (Number.isNaN(date.getTime())) return String(value); return date.toLocaleDateString("en-ZA", { day: "numeric", month: "long", year: "numeric" }); }
function formatEnum(value: unknown) { if (!value) return "Not specified"; return String(value).toLowerCase().replace(/_/g, " ").replace(/\b\w/g, (letter) => letter.toUpperCase()); }

export default function TestsResultsPage() {
  const { data, loading } = useDashboard();
  const laboratory = data?.recentResults?.laboratory ?? [];
  const imaging = data?.recentResults?.imaging ?? [];
  const tests = [...laboratory.map((item) => ({ ...item, recordType: "LABORATORY" })), ...imaging.map((item) => ({ ...item, recordType: "IMAGING" }))];
  const completedCount = tests.filter((test) => test?.result != null || test?.resultValue != null || String(test?.status ?? "").toUpperCase() === "COMPLETED").length;

  return <main className="min-h-screen bg-[#F7F9FC]"><div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8">
    <Link href="/dashboard" className="mb-6 inline-flex items-center gap-2 text-sm font-medium text-[#0B5CAD] hover:underline"><ArrowLeft className="h-4 w-4" />Back to dashboard</Link>
    <div className="mb-8"><div className="mb-3 inline-flex items-center gap-2 rounded-full bg-[#EAF3FB] px-3 py-1 text-xs font-semibold text-[#0B5CAD]"><Activity className="h-3.5 w-3.5" />My health</div><h1 className="text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">Tests & results</h1><p className="mt-2 max-w-2xl text-slate-500">Laboratory tests and imaging studies from your health record.</p></div>
    {loading && <div className="mb-6 rounded-2xl border border-slate-200 bg-white p-4 text-sm text-slate-500">Loading your test results...</div>}
    <section className="mb-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5"><FlaskConical className="h-5 w-5 text-blue-600" /><p className="mt-4 text-2xl font-bold text-slate-900">{tests.length}</p><p className="mt-1 text-sm text-slate-500">Tests & results</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><ImageIcon className="h-5 w-5 text-violet-600" /><p className="mt-4 text-2xl font-bold text-slate-900">{imaging.length}</p><p className="mt-1 text-sm text-slate-500">Imaging studies</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><FileText className="h-5 w-5 text-emerald-600" /><p className="mt-4 text-2xl font-bold text-slate-900">{completedCount}</p><p className="mt-1 text-sm text-slate-500">Results available</p></div></section>
    <section className="mb-8"><div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-semibold text-slate-900">Your results</h2><span className="text-sm text-slate-400">{tests.length} record{tests.length === 1 ? "" : "s"}</span></div>
      {tests.length === 0 ? <div className="rounded-2xl border border-slate-200 bg-white px-6 py-12 text-center"><FlaskConical className="mx-auto h-8 w-8 text-slate-400" /><h3 className="mt-4 font-semibold text-slate-900">No test results yet</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">Laboratory results and imaging studies will appear here when they are added to your health record.</p></div> : <div className="grid gap-4">{tests.map((test: any, index) => { const title = String(test?.name || test?.testName || test?.title || "Health test"); const type = String(test?.recordType || test?.type || test?.category || "TEST"); const date = test?.date || test?.performedAt || test?.completedAt || test?.createdAt; const result = test?.result ?? test?.resultValue ?? test?.value; const status = String(test?.status || "RECORDED"); return <div key={String(test?.id ?? `test-${index}`)} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div className="flex items-start gap-4"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><FlaskConical className="h-5 w-5" /></div><div><h3 className="font-semibold text-slate-900">{title}</h3><p className="mt-1 text-sm text-slate-500">{formatEnum(type)}</p>{date && <p className="mt-2 text-xs text-slate-400">{formatDate(date)}</p>}</div></div><span className="w-fit rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">{formatEnum(status)}</span></div>{result !== undefined && result !== null && result !== "" && <div className="mt-4 rounded-xl bg-slate-50 p-4"><p className="text-xs font-medium text-slate-400">Result</p><p className="mt-1 font-semibold text-slate-900">{String(result)}</p></div>}{(test?.notes || test?.comments || test?.laboratory) && <div className="mt-4 border-t border-slate-100 pt-4">{test?.laboratory && <p className="text-sm text-slate-500">Laboratory: <span className="font-medium text-slate-700">{String(test.laboratory)}</span></p>}{test?.notes && <p className="mt-1 text-sm text-slate-500">{String(test.notes)}</p>}{test?.comments && <p className="mt-1 text-sm text-slate-500">{String(test.comments)}</p>}</div>}</div>; })}</div>}
    </section>
    <section className="mb-8"><div className="rounded-2xl border border-[#D8E7F4] bg-[#F5FAFE] p-6"><div className="flex gap-3"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-[#0B5CAD]" /><div><h3 className="font-semibold text-slate-900">Keep your results together</h3><p className="mt-1 text-sm leading-6 text-slate-500">Store laboratory and imaging information here so you can reference it during future healthcare visits.</p></div></div></div></section>
  </div></main>;
}
